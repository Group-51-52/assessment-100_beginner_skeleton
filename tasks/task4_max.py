def find_max(numbers):
    """
    Finds the largest number in a list of integers.

    Args:
        numbers (list): A list of integers. Assumes the list is non-empty.

    Returns:
        int: The largest number in the list.

    Example:
        >>> find_max([3, 1, 4, 1, 5, 9])
        9
    """
    pass
